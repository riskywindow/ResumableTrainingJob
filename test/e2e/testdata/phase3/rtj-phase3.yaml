# Phase 3 base RTJ template for e2e tests.
#
# Uses the Phase 3 multi-flavor queue (phase3-training).
# Fixed world-size mode (Phase 2 behavior) with flavor-aware launch.
# Lower resource requests than Phase 2 templates to fit more pods on kind.
apiVersion: training.checkpoint.example.io/v1alpha1
kind: ResumableTrainingJob
metadata:
  name: __RTJ_NAME__
  namespace: __DEV_NAMESPACE__
  labels:
    kueue.x-k8s.io/queue-name: __LOCAL_QUEUE_NAME__
    kueue.x-k8s.io/priority-class: phase1-dev
spec:
  suspend: true
  queueName: __LOCAL_QUEUE_NAME__
  workloadPriorityClassName: phase1-dev
  identity:
    image: __TRAINER_IMAGE__
    codeVersion: git:phase3-e2e
    worldSize: 2
    gpuShape: cpu
  runtime:
    mode: DDP
    optimizerMode: adamw
    shardingMode: none
    template:
      apiVersion: jobset.x-k8s.io/v1alpha2
      kind: JobSet
      metadata:
        labels:
          app.kubernetes.io/name: rtj-phase3-e2e
          app.kubernetes.io/component: trainer
      spec:
        replicatedJobs:
          - name: trainer
            replicas: 1
            template:
              spec:
                parallelism: 1
                completions: 1
                backoffLimit: 0
                template:
                  metadata:
                    labels:
                      app.kubernetes.io/name: rtj-phase3-e2e
                  spec:
                    restartPolicy: Never
                    containers:
                      - name: trainer
                        image: __TRAINER_IMAGE__
                        imagePullPolicy: IfNotPresent
                        command:
                          - python
                          - -m
                          - torch.distributed.run
                          - --standalone
                          - --nnodes=1
                          - --nproc-per-node=2
                          - /app/fixtures/pytorch_ddp_counter/train.py
                          - --max-steps
                          - "1000"
                          - --sleep-per-step
                          - "0.2"
                        env:
                          - name: YIELD_SDK_CLUSTER_IDENTITY
                            value: checkpoint-dev
                        envFrom:
                          - secretRef:
                              name: checkpoint-storage-credentials
                        resources:
                          requests:
                            cpu: 250m
                            memory: 256Mi
  checkpoint:
    storageURI: s3://rtj-checkpoints/__RTJ_NAME__/
    interval: 300s
    freshnessBudget: 900s
    maxDrainTime: 180s
    safePointMode: StepBoundary
  resume:
    sourcePolicy: LatestCompatibleComplete
    maxResumeRetries: 3
  control:
    desiredState: Running
